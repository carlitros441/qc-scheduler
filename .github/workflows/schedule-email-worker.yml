name: Schedule Email Worker

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  start-worker:
    runs-on: ubuntu-latest
    steps:
      - name: Start email worker
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run send-emails.yml --repo "${{ github.repository }}"
